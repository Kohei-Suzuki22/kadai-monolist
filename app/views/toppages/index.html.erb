<% content_for :cover do %>

  <div class="cover">
    <div class="cover-inner">
      <div class="cover-contents">
        <h1>すてきなモノと出会う場所</h1>
        <% unless logged_in? %>
          <%= link_to "モノリストを始める", signup_path, class:"btn btn-success btn-lg" %>
        <% end %>
      </div>
    </div>
  </div>

<% end %>

<div id="for-guest">
  <p>
    ※アプリ機能を確認したい方は、loginページからゲストユーザとしてログインしてください。
  </p>
</div>

<%= render "items/items", items: @items %>